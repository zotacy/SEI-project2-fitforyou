<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/app.css"/>
    <link rel="stylesheet" href="/css/profile.css"/>
    <title>New Workout</title>
</head>
<body class='WorkoutBody'>
    <main class='Workout'>
        <h1>New Workout</h1>
        <form class='WorkoutForm' action="/users/profile/<%=user.id%>/workouts" method="POST">
            <label for='activity_name'>Title</label><br/>
            <textarea name="activity_name" placeholder="Title your workout"></textarea>
            
            <label for="exerciseId">Exercise Type</label><br/>
            <select id="exerciseId" name="exerciseId">
                <% for (let i=0; i<exercises.length; i++) { %>
                    <% let selected = ( i == 0) ? "selected" : "";%>
                        <option value="<%=exercises[i].id%>" <%= selected %>> <%= exercises[i].name %> </option>
                <% } %>
            </select><br/>

            <!-- Add difficulties -->
        
            <label for='activity_description'>Description</label><br/>
            <textarea name="activity_description"placeholder="What was your workout?"></textarea>
        
            <label for='activity_notes'>Additional Notes</label><br/>
            <textarea name="activity_notes" placeholder="How did it go? Add special notes here..."></textarea>
            
            <input type="hidden" name="userId" value="<%=user.id%>"/>
            <br/>
            <!-- Submit Button -->
            <input id="submit" type="submit" name="" value="Submit Workout"/>
        </form>
    </main>

    <footer>
        <p id="footer">© 2020 Zach Tacy</p>
    </footer>
</body>
</html>

