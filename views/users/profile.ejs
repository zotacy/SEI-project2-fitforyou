<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/app.css"/>
    <link rel="stylesheet" href="/css/profile.css"/>
    <title>User Profile</title>
</head>
<body>
  <header>
    <h1><%= user.name %>'s Profile</h1>
    
    <!-- Profile info -->
    <form action="/users/profile/<%=user.id%>?_method=PUT" method="POST">
      Name:<input id='name' type="text" name="name" value="<%=user.name%>" /><br/>
      Username:<input id='username' type="text" name="username" value="<%=user.username%>" /><br/>
      Bio:<input id='bio' class="no-outline" type="text" name="bio" value="<%=user.bio%>" /><br />
      <br/>
      <div id="idCheck">
        <% if (user.loggedIn === true) { %>
          <%= user.name %> is logged in.
            <% } else { %>
          <%= user.name %> is NOT logged in.
        <% } %>
      </div>
      <!-- Edit Profile -->
      <input type="submit" value="Edit Profile" />

    </form>

    <!-- Delete Profile -->
    <form action="/users/<%= user.id %>?_method=DELETE" method="POST">
      <input type="submit" value="Delete Profile" />
    </form>

    <br/>
    <a href="/">Back to Homepage</a><br/>
    <a href="/logout"><input type="hidden" name="loggedIn" value="false"/>Log Out</a><br/>
    <a href="/users/newWorkout">New Workout</a>
  </header>

  <main>
    <div class="exercises">
        <div class="exerciseBlock">
          <div class="row">
          <div class="column">
              <div class="card">
                <a href="/users/showWorkout"><img src="https://i.imgur.com/7z8ah4B.png?1" title="source: imgur.com"  alt="Blue Shoes"/></a>
                  <h2>Running</h2>
                  <div class="container">
                      <p class='diffCount'>Easy:</p>
                      <p class='diffCount'>Moderate:</p>
                      <p class='diffCount'>Hard:</p>
                  </div>
                  <a class="button" href="/users/newWorkout"><button type="button">Add New Workout</button></a>
              </div>
          </div>
          </div>
        </div>

        <div class="exerciseBlock">
          <div class="row">
          <div class="column">
              <div class="card">
                <a href="/users/showWorkout"><img src="https://i.imgur.com/jXWvolq.png?1" title="source: imgur.com" alt="QR Bike (black)"/></a>
                  <h2>Cycling</h2>
                  <div class="container">
                      <p class='diffCount'>Easy:</p>
                      <p class='diffCount'>Moderate:</p>
                      <p class='diffCount'>Hard:</p>
                  </div>
                  <a class="button" href="/users/newWorkout"><button type="button">Add New Workout</button></a>
              </div>
          </div>
          </div>
        </div>

        <div class="exerciseBlock">
          <div class="row">
          <div class="column">
              <div class="card">
                  <a href="/users/showWorkout"><img src="https://i.imgur.com/3vX9a3N.jpg?1" title="source: imgur.com" alt="Swimming Pool" /></a>
                  <h2>Swimming</h2>
                  <div class="container">
                      <p class='diffCount'>Easy:</p>
                      <p class='diffCount'>Moderate:</p>
                      <p class='diffCount'>Hard:</p>
                  </div>
                  <a class="button" href="/users/newWorkout"><button type="button">Add New Workout</button></a>
              </div>
          </div>
        </div>
        </div>
    </div>
  </main>

<footer>
        <p id="footer">© 2020 Zach Tacy</p>
    </footer>
</body>
</html>